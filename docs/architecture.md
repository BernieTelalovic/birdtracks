# Architecture

The package is divided into permutation values and operations, exact linear
combinations, and a reserved projector layer. A readable Python backend will
serve as the semantic reference for future optimized backends.

