# Notation

A cycle `(a,b,c)` maps `a` to `b`, `b` to `c`, and `c` to `a`.
Products act right-to-left: `p * q` first applies `q`, then `p`. A permutation
has finite support; every integer not stored in its sparse mapping is fixed.
